{
  "name": "interview",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "babel-plugin-transform-object-rest-spread": "^6.6.5",
    "babelify": "^7.2.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "lodash": "^4.6.1",
    "react": "^0.14.6",
    "react-dom": "^0.14.6"
  },

  "devDependencies": {
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/watchify/bin/cmd.js app.js -o bundle.js -t [ babelify --presets [ es2015 react ] --plugins [transform-object-rest-spread] ] -v"
  },
  "author": "",
  "license": "ISC"
}
